{ "contract": "project-init", "kind": "implementation", "imports": [], "files": ["tools/init.mjs"], "notes": "Validates before writing so a refused init leaves the target untouched; --force adds without overwriting. Scaffolds the kernel, a root contract, a structural starter gate, laws, CI, and PROTOCOL.md. Gate erratum caught during authoring: the bad-name test passed while the tool was missing (the crash text happened to contain 'name') — the gate now rejects crash signatures explicitly." }
